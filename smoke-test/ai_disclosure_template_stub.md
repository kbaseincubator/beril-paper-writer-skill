Manuscript drafting was performed with the BERIL paper-writer skill (`beril-paper-writer v0.1.0-preview`), using `claude-sonnet-4-5` via the Claude Code harness. Inputs were the project artifacts at `projects/functional_dark_matter/` (snapshot SHA: `smoke-test`). The throughline was selected by the human author from candidates surfaced by the writer (smoke-test note: hand-crafted single candidate for this run, since `plan.v1` was not invoked). Methods were extracted from the project's notebooks via `extract_methods.py`'s AST walker; statistical claims were verified against the extracted provenance file. Citations were drawn from a literature pool with DOI/PMID verification (pool produced by a separate `citation_pool.v1` run on 2026-04-25). The draft was reviewed adversarially using `beril-adversarial v0.1.0-preview` and revised over 0 pass(es). The human authors reviewed and edited the final manuscript and accept full responsibility for its content. No AI tool is listed as an author.
