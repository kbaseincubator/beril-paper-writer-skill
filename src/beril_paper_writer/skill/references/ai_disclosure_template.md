Manuscript drafting was performed with the BERIL paper-writer skill (`beril-paper-writer v{paper_writer_version}`), using `{model_id}` via the Claude Code harness. Inputs were the project artifacts at `projects/{project_id}/` (snapshot SHA: `{sha}`). The throughline was selected by the human author from candidates surfaced by the writer. Methods were extracted from the project's notebooks via `extract_methods.py`'s AST walker; statistical claims were verified against the extracted provenance file. Citations were drawn from a literature pool with DOI/PMID verification produced by a separate `citation_pool.v1` run. The draft was reviewed adversarially using `beril-adversarial` and revised over {N} pass(es). The human authors reviewed and edited the final manuscript and accept full responsibility for its content. No AI tool is listed as an author.
